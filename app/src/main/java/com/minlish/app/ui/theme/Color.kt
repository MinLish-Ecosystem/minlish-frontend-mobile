package com.minlish.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Material baseline (giữ để Theme.kt ColorScheme không lỗi) ───────────────
val Purple80      = Color(0xFFD0BCFF)
val PurpleGrey80  = Color(0xFFCCC2DC)
val Pink80        = Color(0xFFEFB8C8)
val Purple40      = Color(0xFF6650a4)
val PurpleGrey40  = Color(0xFF625b71)
val Pink40        = Color(0xFF7D5260)

// ── MinLish Brand — Primary ──────────────────────────────────────────────────
val MinlishPrimary     = Color(0xFF4648D4)
val MinlishPrimary900  = Color(0xFF312E81)   // tông đậm hơn, ProfileScreen dùng

// ── MinLish Brand — Gradient ─────────────────────────────────────────────────
val MinlishGradientStart = Color(0xFF667EEA)
val MinlishGradientEnd   = Color(0xFF764BA2)

// ── Surface ──────────────────────────────────────────────────────────────────
val MinlishSurface              = Color(0xFFFCF8FF)
val MinlishSurfaceContainer     = Color(0xFFEFECF8)
val MinlishSurfaceContainerHigh = Color(0xFFE9E6F3)
val MinlishSurfaceContainerLow  = Color(0xFFF5F2FE)
val MinlishSurfaceLowest        = Color(0xFFFFFFFF)
val MinlishSurfaceHighest       = Color(0xFFE4E1ED)

// ── On Surface ───────────────────────────────────────────────────────────────
val MinlishOnSurface        = Color(0xFF1B1B23)
val MinlishOnSurfaceVariant = Color(0xFF464554)

// ── Outline ──────────────────────────────────────────────────────────────────
val MinlishOutline        = Color(0xFF767586)
val MinlishOutlineVariant = Color(0xFFC7C4D7)

// ── Semantic ─────────────────────────────────────────────────────────────────
val MinlishSuccess = Color(0xFF10B981)
val MinlishWarning = Color(0xFFF59E0B)
val MinlishError   = Color(0xFFEF4444)

// ── Accent (Analytics Screen) ─────────────────────────────────────────────────
val MinlishInfo       = Color(0xFF3B82F6)   // Timer icon trong Analytics
val MinlishAccentCyan = Color(0xFF06B6D4)   // Mastery Beginner bar màu cyan

// ── Secondary / Tertiary — dùng cho Theme.kt ColorScheme ─────────────────────
val MinlishSecondary            = Color(0xFF8127CF)
val MinlishOnSecondary          = Color(0xFFFFFFFF)
val MinlishSecondaryContainer   = Color(0xFF9C48EA)
val MinlishOnSecondaryContainer = Color(0xFFFFFBFF)
val MinlishTertiary             = Color(0xFF904900)
val MinlishOnTertiary           = Color(0xFFFFFFFF)
val MinlishTertiaryContainer    = Color(0xFFB55D00)

// ── Inverse — dùng cho Theme.kt ColorScheme ──────────────────────────────────
val MinlishInversePrimary  = Color(0xFFC0C1FF)
val MinlishInverseSurface  = Color(0xFF303038)
val MinlishInverseOnSurface = Color(0xFFF2EFFB)
